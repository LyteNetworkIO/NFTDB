function nftdb(data, operation) {

    //iterate over the array of paymails, and pay out the respective amount.
    
    console.log(data+" has been processed via "+operation);
}

module.exports = nftdb